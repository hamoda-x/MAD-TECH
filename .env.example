# MAD_TECH Monorepo
# Copy environment files to each workspace:
#   backend/.env      ← DATABASE_URL, NEXTAUTH_SECRET, WHATSAPP_NUMBER
#   frontend/.env.local ← NEXTAUTH_SECRET, BACKEND_URL, Cloudinary keys
#
# See backend/.env.example and frontend/.env.example for details.
